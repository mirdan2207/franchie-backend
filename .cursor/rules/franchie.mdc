---
description: 
globs: 
alwaysApply: true
---
📘 Техническое задание (ТЗ): Backend системы управления франчайзингом
🧱 1. Архитектура
Тип: Монолит с возможностью запуска отдельных частей как микросервисов.

Язык программирования: Node.js.

Web-сервер: Nginx (Reverse Proxy).

База данных: PostgreSQL.

ORM: Prisma.

Аутентификация и авторизация:

JWT + RBAC (Role-Based Access Control).

Логирование:

Middleware логирует действия пользователей в PostgreSQL.

Интеграция с Prometheus и Grafana для сбора, хранения и визуализации логов и метрик.

👥 2. Роли пользователей
2.1. Админ (франчайзер)
Создает партнера, назначает точку (заведение), выдает логин/пароль.

Имеет полный доступ к административной панели.

Просматривает общую аналитику по партнерам, заведениям.

Может блокировать доступ партнера.

2.2. Партнер (владелец точки)
Входит в систему по учетке, выданной админом.

Видит только свои заведения.

Управляет сотрудниками заведения: создает логин/пароль (общий) для входа, а также добавляет/удаляет профили сотрудников.

Управляет меню: добавление, редактирование, загрузка утвержденных позиций.

Аналитика: выручка, история заказов, отзывы, рейтинг сотрудников.

2.3. Сотрудник
Входит в систему по общему логину точки (выдан партнером).

Выбирает себя из списка профилей (идентификация).

Выполняет заказы.

Имеет доступ только к данным, необходимым для выполнения задач.

Не видит аналитику и данные других сотрудников.

🔄 3. Последовательность бизнес-процессов
A. Подключение нового партнера:
Партнер подписывает документы с франчайзером.

Админ вручную в системе:

Создает аккаунт партнера.

Назначает заведение (создает точку).

Выдает логин/пароль.

B. Работа партнера:
Входит по логину/паролю.

Создает общий логин/пароль для планшета.

Добавляет сотрудников (профили).

Управляет меню, следит за аналитикой, просматривает отзывы.

C. Работа сотрудников:
Вход через планшет (по общему логину).

Выбор своего имени в списке.

Работа с заказами.

Логи действий сохраняются в БД.

4.2. Управление пользователями
Админ:

создать партнера.

создать заведение.

Партнер:

создать профили сотрудников.

создать логин/пароль для устройства.

4.3. Меню и акции
Партнер может:

CRUD меню и акций.

4.4. Работа сотрудников
Авторизация по устройству.

Выбор имени.

Обработка заказов (пример: POST /orders).

Отображение заказов, статусов.

4.5. Отзывы
POST /feedback — посетитель к примеру сканирует qr код со своего чек, заполняет форму и оставляет отзыв.

Партнер видит только свои отзывы.

4.6. Аналитика
Выручка, заказы, отзывы, рейтинг.

Метрики по точкам.

Доступ:

Админ — ко всем точкам.

Партнер — к своим.

📊 5. Логирование и мониторинг
Middleware логирует:

UserID, роль, endpoint, метод, время.

Пишется в таблицу logs в PostgreSQL.

Prometheus собирает метрики:

Кол-во запросов, ошибки, отклики.

Grafana визуализирует данные:

Дашборды по партнерам, точкам, метрикам запросов.

🔌 6. Инфраструктура
Node.js backend (REST API).

Nginx — reverse proxy (балансировка).

Docker + Docker Compose для локального запуска.

Prometheus + Grafana — мониторинг и метрики.

PostgreSQL — основное хранилище.

Swagger (OpenAPI) — автогенерация документации.

